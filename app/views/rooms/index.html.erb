<h1>部屋一覧</h1>

<%= render "search" %>

<table class="list">
  <tr>
    <td>部屋番号</td>
  </tr>
  <% Array(@rooms).each do |room| %>
    <tr>
      <td><%= link_to room.room_number, room %></td>
    </tr>
  <% end %>
</table>