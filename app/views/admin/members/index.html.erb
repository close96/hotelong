<h1>会員一覧</h1>

<table class="list">
  <tr>
    <th>ユーザーID</th>
  </tr>
  <% @members.each do |member| %>
    <tr>
      <td><%= link_to member.user_id, [:admin, member] %></td>
    </tr>
  <% end %>
</table>
<%= will_paginate @plans %>