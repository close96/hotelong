<h1>登録確認</h1>

<%= form_for [:admin, @plan] do |form| %>
  <table class="attr">
    <tr>
      <th><%= form.label :plan_name %></th>
      <td><%= @plan.name %></td>
      <td><%= form.hidden_field :name %></td>
    </tr>
    <tr>
      <th><%= form.label :apply_count %></th>
      <td><%= @plan.apply_count %>人</td>
      <td><%= form.hidden_field :apply_count %></td>
    </tr>
    <tr>
      <th><%= form.label :price %></th>
      <td><%= @plan.price %></td>
      <td><%= form.hidden_field :price %></td>
    </tr>
  </table>

  <p>この内容で登録しますか？</p>
  
  <p><%= form.submit "登録する" %></p>
<% end %>

<%= link_to "管理者トップへ", :admin_root %>